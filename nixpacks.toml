# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ['dotnet-sdk_8']

[phases.build]
cmds = [
    'dotnet publish backend/src/Api/Api.csproj -c Release -o /app/out'
]

[start]
cmd = './out/Api'
